# m6AsynDriver
m6AsynDriver is the first tool for identifying potential m6A-associated synonymous driver mutations by integrating multi-omics datasets. m6AsynDriver employs a Row-Attention Transformer with Self-supervised Learning (RASL) module to learn informative representations from input features and used the XGBoost as classifier.. 
